#include <stdio.h>
#include <stdlib.h>

char arr[100005];

int main()
{
    int test,n,k,i,ones,zeros;
    scanf("%d",&test);
    while(test--)
    {
        ones = zeros = 0;
        scanf("%d %d",&n,&k);
        scanf("%s",arr);

        if(k==1)
        {

        }
        else
        {
            for()
        }
    }
    return 0;
}
